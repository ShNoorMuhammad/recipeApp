

<%@items.each do |item|%>
<div class="row">
<div class="col-md-2">
<section class=center>

</section>
</div>

<div class="col-md-8 well"> 
<h4><%= link_to item.name, item_path(item) %></h4>
    <p><%= item.description%></p>
<% if logged_in? && current_chef == item.chef%>
<%= link_to "Edit this recipe", edit_item_path(item), class: 'btn btn-xs btn-warning' %>
     <%= link_to "Delete this recipe" , item_path(item), data: { turbo_method: :delete } , class: 'btn btn-xs btn-danger' %>


<%end%>
    </div>

    
    </div>

<%end%>